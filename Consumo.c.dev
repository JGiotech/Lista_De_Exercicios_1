#include <stdio.h>

int main() {
  int km;
  double litro;

 	scanf("%d", &km);
  scanf("%lf", &litro);
   
 
  double consumo = km / litro;

 
  printf("%.3lf km/l\n", consumo);

return 0;
}
